# 🚀 部署指南

## 快速开始

### 本地开发运行

```bash
# 进入项目目录
cd draw-guess-game

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 打开浏览器访问 http://localhost:5173
```

## 功能说明

### ✅ 已实现功能

1. **用户系统**
   - 昵称设置
   - 头像选择（32个可爱emoji表情）

2. **房间系统**
   - 创建房间（自动生成6位邀请码）
   - 加入房间（输入邀请码）
   - 房主管理和开始游戏

3. **游戏核心**
   - Canvas绘画功能
   - 工具栏（颜色、笔刷大小、撤销/重做/清空）
   - 实时绘画同步（本地演示）
   - 计时器（可配置30-120秒）
   - 轮流作画机制

4. **聊天和猜词**
   - 实时聊天消息
   - 智能答案判定（支持相似词检测）
   - 自动计分
   - 聊天历史记录

5. **排行榜**
   - 实时显示玩家得分
   - 排名显示（奖牌装饰）

6. **UI设计**
   - 温馨配色（粉红、橙色、紫色、蓝色）
   - 毛玻璃效果
   - 发光渐变背景
   - 流畅的动画过渡
   - 响应式设计

### 🔄 本地多标签页测试

由于项目使用本地状态管理，你可以：

1. 打开多个浏览器标签页
2. 在第一个标签页创建房间
3. 在其他标签页用邀请码加入
4. 模拟多人游戏

**注意**：本地演示只能同一设备的标签页通信。

## Firebase集成（远程联机）

### 为什么需要Firebase？

本地版本只能在同一设备的标签页间通信。要实现真正的远程联机，需要使用Firebase：

1. **跨设备同步** - 不同电脑/手机的玩家可以联机
2. **实时数据库** - 同步绘画、聊天、游戏状态
3. **持久化存储** - 保存游戏记录

### Firebase配置步骤

#### 1. 创建Firebase项目

```
访问 https://console.firebase.google.com
-> 创建项目
-> 输入项目名称
-> 创建Firebase项目
```

#### 2. 启用Realtime Database

```
在Firebase Console中：
-> 创建数据库
-> 选择位置（如 us-central1）
-> 开启为测试模式
```

#### 3. 获取配置信息

```
项目设置 -> Web应用 -> 注册应用
复制显示的配置对象
```

#### 4. 配置环境变量

创建文件 `.env.local`：

```env
VITE_FIREBASE_API_KEY=xxxxxxxxxxxxxxxxx
VITE_FIREBASE_AUTH_DOMAIN=yourproject.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=yourproject
VITE_FIREBASE_STORAGE_BUCKET=yourproject.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef123456
VITE_FIREBASE_DATABASE_URL=https://yourproject.firebaseio.com
```

#### 5. 启动应用

```bash
npm run dev
```

现在你的应用会自动连接Firebase！

## GitHub Pages部署

### 1. 准备GitHub仓库

```bash
# 如果还没有初始化git
git init

# 添加远程仓库
git remote add origin https://github.com/your-username/draw-guess-game.git

# 创建main分支
git branch -M main
```

### 2. 构建生产版本

```bash
# 构建项目
npm run build

# 验证dist目录已生成
ls dist
```

### 3. 部署到GitHub Pages

方式A：使用GitHub Actions（推荐）

```bash
# 推送到GitHub
git add .
git commit -m "Initial commit: Draw and Guess Game"
git push -u origin main
```

然后在GitHub设置中启用Pages

方式B：手动部署

```bash
# 安装gh-pages
npm install --save-dev gh-pages

# 添加到package.json脚本
# "deploy": "npm run build && gh-pages -d dist"

# 部署
npm run deploy
```

### 4. 访问你的游戏

```
https://your-username.github.io/draw-guess-game
```

## 自定义配置

### 修改词库

编辑 `src/data/wordBank.ts`，添加你自己的词汇：

```typescript
export const wordBank: WordItem[] = [
  { word: '你的词汇', difficulty: 'easy', category: '分类' },
  // ...
];
```

### 调整游戏参数

在 `src/App.tsx` 中修改：
- 房间最大玩家数（当前6人）
- 默认轮数和时间
- 计分规则

### 修改UI主题

在 `tailwind.config.js` 中修改：
- 颜色方案
- 动画速度
- 字体选择

## 常见问题

### Q: 为什么本地版本只能一个设备？
A: 本地版本使用浏览器内存存储，每个标签页是独立的。需要Firebase才能跨设备同步。

### Q: Firebase免费额度够吗？
A: 是的，Firebase免费额度足够小型游戏使用（每月读写限制充足）。

### Q: 如何修改游戏中的词汇？
A: 编辑 `src/data/wordBank.ts` 文件，添加新词汇到数组中。

### Q: 支持多少玩家同时游戏？
A: 当前配置支持2-6个玩家，可以修改代码增加人数。

### Q: 如何在手机上玩？
A: 部署到GitHub Pages或其他服务器后，用手机访问网址即可。

## 下一步优化

1. **音效系统** - 添加背景音乐和效果音
2. **游戏回放** - 保存游戏过程并回放
3. **自定义题库** - 房主可以添加自己的词汇
4. **观众模式** - 允许旁观者观看游戏
5. **排名系统** - 保存历史排名
6. **多语言** - 支持不同语言

## 支持和反馈

如遇到问题，请检查：

1. ✅ Node.js版本（需要14+）
2. ✅ npm包是否完整安装
3. ✅ Firebase配置是否正确
4. ✅ 浏览器是否支持Canvas API
5. ✅ 网络连接是否正常

---

**祝你游戏愉快！🎉**
