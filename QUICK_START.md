# ⚡ 快速参考指南

## 🎮 游戏快速开始（3分钟）

### 1️⃣ 安装和启动
```bash
cd draw-guess-game
npm install
npm run dev
```

### 2️⃣ 打开浏览器
访问 `http://localhost:5173`

### 3️⃣ 多人测试
```bash
# 打开多个浏览器标签页
Tab 1: 创建房间 → 获得邀请码
Tab 2: 加入房间 → 输入邀请码
```

## 🎯 游戏规则速览

| 阶段 | 描述 | 时间 |
|------|------|------|
| 设置 | 输入昵称和选择头像 | 1分钟 |
| 大厅 | 等待所有玩家准备好 | 2分钟 |
| 游戏 | 轮流绘画和猜测 | 1-2分钟/轮 |
| 结算 | 显示最终排行榜 | 1分钟 |

## 🎨 绘画工具使用

| 工具 | 快捷键/方式 | 说明 |
|------|-----------|------|
| 笔刷 | 滑块 | 调整1-20px粗细 |
| 颜色 | 颜色选择器 | 任意颜色 |
| 撤销 | ↶ 按钮 | 上一步 |
| 重做 | ↷ 按钮 | 下一步 |
| 清空 | 🗑️ 按钮 | 清空整个画布 |

## 💬 聊天快捷键

| 键 | 功能 |
|----|------|
| Enter | 发送消息 |
| Shift+Enter | 换行 |

## 🏆 计分规则

```
猜对一次 = 50分
绘画者额外 = 20分

例子：
- 玩家A绘画，玩家B猜对 → B得50分，A得20分
```

## 🌟 配色参考

```css
主色调：
- 温馨粉红 #FFB6C1
- 温暖橙色 #FFCC80
- 柔和紫色 #E1BEE7
- 清爽蓝色 #B3E5FC
```

## 📁 重要文件位置

| 文件 | 用途 |
|------|------|
| `src/App.tsx` | 主应用逻辑 |
| `src/components/` | React组件 |
| `src/data/wordBank.ts` | 词库（可编辑） |
| `vite.config.ts` | Vite配置 |
| `tailwind.config.js` | Tailwind配置 |

## 🔧 常用命令

```bash
# 开发
npm run dev          # 启动开发服务器

# 生产构建
npm run build        # 打包生产版本
npm run preview      # 预览生产版本

# 代码检查
npm run lint         # 检查代码风格
```

## 🚀 部署三步走

```bash
# 1. 构建
npm run build

# 2. 推送到GitHub
git add . && git commit -m "v1.0" && git push

# 3. 启用GitHub Pages
# GitHub Settings → Pages → main/dist → Save
```

## 🎓 代码片段

### 添加新词汇
```typescript
// src/data/wordBank.ts
export const wordBank: WordItem[] = [
  { word: '新词', difficulty: 'easy', category: '分类' },
  // ...
];
```

### 修改颜色
```css
/* src/index.css */
:root {
  --warm-pink: #新颜色;
  --warm-orange: #新颜色;
  // ...
}
```

### 修改房间人数
```typescript
// src/components/Room/JoinRoom.tsx
if (room.players.length >= 新数字) {  // 改成你想要的人数
  setJoinError('房间已满');
}
```

## 🐛 调试技巧

### 浏览器控制台
```javascript
// 查看游戏状态
console.log('当前游戏状态:', gameState);

// 清除本地缓存
localStorage.clear();
sessionStorage.clear();
```

### React DevTools
安装浏览器扩展：
- Chrome: React Developer Tools
- Firefox: React Developer Tools

## 📞 常见问题速查

| 问题 | 解决 |
|------|------|
| 网页无法加载 | 检查 `npm run dev` 是否运行 |
| 样式很奇怪 | 清除浏览器缓存，刷新页面 |
| 无法加入房间 | 确保邀请码正确（6位） |
| 绘画很卡 | 关闭其他标签页，释放内存 |
| 消息没有出现 | 刷新页面，重新加入房间 |

## 📱 移动设备支持

✅ iOS Safari - 支持
✅ Android Chrome - 支持
⚠️ 绘画在小屏幕上可能困难
💡 建议在平板或电脑上游玩

## 🎪 高级玩法

1. **自定义词库** - 编辑 wordBank.ts 添加你们的词汇
2. **修改配色** - 在 tailwind.config.js 中自定义颜色
3. **调整时间** - 创建房间时选择不同的轮时间
4. **增加轮数** - 创建房间时调整轮数

## 📊 性能优化

如果游戏运行缓慢：

1. 关闭浏览器其他标签页
2. 减少浏览器扩展
3. 使用 Chrome 而不是其他浏览器
4. 清除浏览器缓存

## 🔐 安全提示

✅ 所有数据在本地处理
✅ 无第三方广告追踪
✅ 支持离线游戏（本地模式）
⚠️ 启用Firebase后遵守其隐私政策

## 🌍 部署地点汇总

| 平台 | 优点 | 缺点 |
|------|------|------|
| GitHub Pages | 免费、简单 | 只支持静态 |
| Vercel | 快速、免费 | 需要账号 |
| Netlify | 功能丰富 | 需要账号 |
| 自己的服务器 | 完全控制 | 需要维护 |

## 💡 开发建议

### 代码风格
```typescript
// ✅ 好
const handleClick = () => { /* ... */ };

// ❌ 避免
function handleClick() { /* ... */ }
```

### 组件命名
```typescript
// ✅ 好
export default function GameBoard() {}

// ❌ 避免
export default function gameBoard() {}
```

## 🎯 下一个目标

1. 📹 录制游戏过程
2. 🎵 添加音效
3. 🎪 创建观众模式
4. 📊 保存游戏统计

---

**快速上手，开始游戏！** 🎮✨
